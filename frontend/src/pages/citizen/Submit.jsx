// Owner: citizen-UI track (see TASKS.md). Talks to POST /complaints via ../../api/client.js.
export default function CitizenSubmit() {
  return (
    <div style={{ padding: 16 }}>
      <h1>Report an issue</h1>
      <p>TODO: form (category, description, location, address) -&gt; POST /complaints</p>
    </div>
  );
}
