// Owner: admin-UI track (see TASKS.md). Talks to GET /complaints and GET /analytics/summary.
export default function AdminDashboard() {
  return (
    <div style={{ padding: 16 }}>
      <h1>Admin dashboard</h1>
      <p>TODO: queue table + charts -&gt; GET /complaints, GET /analytics/summary</p>
    </div>
  );
}
